# API base URL (must be prefixed with VITE_ to expose to the browser)
# Use /api/v1 with Vite proxy in dev, or full URL for production
VITE_API_BASE_URL=/api/v1

# App metadata
VITE_APP_NAME=AI Codebase Intelligence
